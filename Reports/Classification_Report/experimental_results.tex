\section{Experimental Results}
\label{sec:experimental_resuts}
This section presents the classification performance of the various machine learning models using the selected gyroscope-based features. 

\subsection{Model Accuracy and AUC}
Fig.~\ref{fig:accuracy_barplot} shows the classification accuracy of each model using the selected features: minimum and maximum values of the 
z-axis angular velocity from the left and right leg. The models tested include Logistic Regression, Random Forest, Support Vector Machines (SVM) 
with linear, polynomial, and RBF kernels, XGBoost, and K-Nearest Neighbors (KNN). 




The highest accuracy (94.44\%) was achieved by \textit{KNN}, 
\textit{Logistic Regression}, and \textit{SVM (Linear/RBF Kernel)}. In contrast, \textit{XGBoost} yielded the lowest accuracy (77.78\%).
As shown in Table~\ref{tab:metrics}, these high-performing models also achieved an AUC of 1.00, indicating perfect separability in LOOCV, while 
XGBoost underperformed with an AUC of 0.60.

\begin{table}[htbp]
    \caption{Accuracy and AUC for All Classifiers Using LOOCV}
    \label{tab:metrics}
    \centering
    \begin{tabular}{lcc}
    \toprule
    \textbf{Model} & \textbf{Accuracy} & \textbf{AUC} \\
    \midrule
    Logistic Regression     & 0.944 & 1.000 \\
    Random Forest           & 0.833 & 0.975 \\
    SVM (Linear Kernel)     & 0.944 & 1.000 \\
    SVM (RBF Kernel)        & 0.944 & 1.000 \\
    SVM (Polynomial Kernel) & 0.889 & 0.951 \\
    XGBoost                 & 0.778 & 0.605 \\
    KNN                     & 0.944 & 1.000 \\
    \bottomrule
    \end{tabular}
\end{table}

% \subsection{ROC Curve Analysis}
% Fig.~\ref{fig:roc_curves} presents the ROC curves for all classifiers. \textit{KNN}, \textit{SVM (RBF/Linear)}, and \textit{Logistic Regression} show 
% perfect discrimination with an AUC of 1.00. The \textit{Random Forest} model closely follows with an AUC of 0.975. The underperformance of \textit{XGBoost} 
% is clearly visible with a notably flatter curve, suggesting difficulties in threshold-based separation.

% \begin{figure}[htbp]
% \centering
% \includegraphics[width=\linewidth]{selected_features_models_roc_curves.png}
% \caption{ROC curves for all models using selected gyroscope features.}
% \label{fig:roc_curves}
% \end{figure}

% \subsection{Confusion Matrix Evaluation}

% Fig.~\ref{fig:confusion_matrices} illustrates the confusion matrices for each classifier. Most models performed well on both classes, showing strong 
% diagonal dominance. Notably, \textit{KNN}, \textit{Logistic Regression}, and \textit{SVM (Linear/RBF)} each misclassified only one instance, yielding 
% very low false positive and false negative rates. In contrast, \textit{XGBoost} and \textit{Random Forest} demonstrated more balanced misclassification 
% across both classes.

% \begin{figure*}[htbp]
% \centering
% \includegraphics[width=0.9\textwidth]{confusion_matrix_panel.png} % Composite image if available
% \caption{Confusion matrices for all classifiers. True class labels are shown on the vertical axis; predicted labels on the horizontal axis.}
% \label{fig:confusion_matrices}
% \end{figure*}

% \subsection{Summary of Findings}

% The overall results validate that even with minimal statistical features from gyroscope data, machine learning models can effectively classify normal versus 
% abnormal gait with high accuracy. The best-performing models—\textit{KNN}, \textit{Logistic Regression}, and \textit{SVMs}—are also lightweight, making them 
% suitable for integration into wearable or mobile healthcare systems. The performance consistency across LOOCV folds also indicates robustness to individual 
% variability, which is crucial for real-world deployment.










